name: CI - Audit Tests

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test-audit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Audit Tests
        run: PYTHONPATH=$PWD pytest tests/test_audit.py
